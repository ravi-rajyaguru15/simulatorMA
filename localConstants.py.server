import os
from datetime import datetime

REPEATS = 6
DRAW_GRAPH = False
SAVE_GRAPH = True
OUTPUT_DIRECTORY = "/output/simulator/" + str(datetime.now()).replace(":", ".") + "/"
DEBUG_FILE = False
DEBUG_HISTORY = False

# try: os.makedirs(OUTPUT_DIRECTORY, exist_ok=True)
# except FileExistsError: pass